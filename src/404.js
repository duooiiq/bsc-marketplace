export default function NotFound() {
    return (
        <div className="NotFoundContainer">
            <h2>Lo siento el item que estas buscando no ha sido encontrado por favor revisa si el la URL del item esta correcta</h2>
            <b>
                <h1>La URL es parecida a la siguiente:</h1>
                <code>https://goldennfts.net/nft/tokenId/(ID_DEL_TOKEN)</code>
            </b>
        </div>
    )
}